import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-setup-complete',
  imports: [
    RouterLink
  ],
  templateUrl: './setup-complete.html',
  styleUrl: './setup-complete.scss',
})
export class SetupComplete {}
